import { DiscountPriceDirective } from './discount-price.directive';

describe('DiscountPriceDirective', () => {
  it('should create an instance', () => {
    const directive = new DiscountPriceDirective();
    expect(directive).toBeTruthy();
  });
});
